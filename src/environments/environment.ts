// used in dev, will get eplaced by the appropriate file variation during:
// ng build --configuration=production

export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
};
